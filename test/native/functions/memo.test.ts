import { expectType } from 'tsd'

// test
// import { memo } from '../../../src/native/functions/memo'


describe('native/functions/memo.ts', () => {
  describe('memo()', () => {
    it.skip('should return a typed memoized function', () => {
      // expectType<number>(memo(1))
    })

    it.skip('should memoize', () => {
      // expect(memo(1)).toEqual(1)
    })
  })
})
